# dependencies / build
node_modules/
dist/
build/
.wrangler/
.turbo/
coverage/
*.tsbuildinfo

# env & secrets — never commit
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example
*.pem
*.key

# local gateway runtime artifacts (attester key is covered by *.key above)
*.db
*.db-journal
*.sqlite
scopebond-receipts.jsonl

# private design docs must never live in this public repo.
# (Belt-and-suspenders: the oss-gate also blocks these paths and their content.)
/docs/
/CLAUDE.md
/AGENTS.private.md
/changelog/
# local-only context / scratch for working across the two repos (never committed)
/.context/

# local tooling
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/*
!.vscode/extensions.json

# scratch
tmp/
.clean-validation-dev02*/
