# Changesets

This folder holds [changesets](https://github.com/changesets/changesets) — one
Markdown file per pending change, describing which packages to bump and how. Add
one with `pnpm changeset`. See [`RELEASING.md`](../RELEASING.md).
